name: Build Binder Notebook Container

on:
  push:
    branches:
    - main
    - echuraev/binder_configuration

jobs:
  trigger-binder-build:
    runs-on: ubuntu-latest
    steps:
    - uses: s-weigand/trigger-mybinder-build@v1
      with:
        target-repo: echuraev/EmotiEffLib/${{ github.ref }}
